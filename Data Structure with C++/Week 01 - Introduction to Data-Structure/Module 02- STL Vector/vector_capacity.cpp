#include <bits/stdc++.h>
using namespace std;
int main()
{
    // v.capacity()
            // vector<int> v;
            // cout << v.capacity() << endl; // show the capacity
            // v.push_back(10);
            // cout << v.capacity() << endl; // show the capacity

    // v.size()
            // vector<int> v(10);
            // cout << v.size() << endl;

            // v.max_size()
            //  vector<int> v;
            //  cout << v.max_size() << endl;

    // v.clear()
            //  vector<int> v ={10,20,30,40,60,910,550,55};
            //  cout << v.size() <<endl;
            //  for(int i=0;i<v.size();i++)
            //  {
            //      cout << v[i] << " ";
            //  }
            //  v.clear(); //clear the size only ..not the value of index
            //  cout << endl << v.size() <<endl;
            //  for(int i=0;i<8;i++)
            //  {
            //      cout << v[i] << " ";
            //  }

    // v.resize()
        //      vector<int>v;
        //      cout << v.size() << endl;
        //      v.resize(10,99);
        //     // v.resize(2);
        //      //v.resize(10);
        //      cout << v.size() << endl;
        //      for(int i=0;i<v.size();i++)
        //      {
        //          cout << v[i] <<" ";
        //      }
    //v.empty()
        //     vector<int> v;
        //     //cout << v.empty() << endl;
        //     if(v.empty()==true)
        //     {
        //         cout << "vector was chi ching fak" << endl;
        //     }
        //     vector<int> v2={10,20,50,040,60,50,005};
        //     //cout << v2.empty() << endl;
        //     if(v2.empty()==false)
        //     {
        //         cout << "the vector is vorpur" <<endl;
        //     }
    return 0;
}