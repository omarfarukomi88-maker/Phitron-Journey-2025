#include <stdio.h>
int main()
{
    int tk;
    scanf("%d", &tk);
   
    if (tk >= 10000)
    {
        printf("Coxs-Bazar Jabo and then\n");
        if (tk >= 15000)
        {
            printf("Sent-martin jabo\n");
        }
        else
        {
            printf("Ferot chole ashbo\n");
        }
    }
    else
    {
        printf("Kothaw jabo na\n");
    }
    return 0;
}